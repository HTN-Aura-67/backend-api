# Allow pre-releases/nightlies, and point to PyTorch nightly CPU index
--pre
--index-url https://download.pytorch.org/whl/nightly/cpu
--extra-index-url https://pypi.org/simple

# PyTorch packages (nightly CPU wheels)
torch
torchvision
torchaudio

# Common libs
numpy
pyyaml
tqdm
termcolor
matplotlib

# Using headless avoids mac GUI issues and reduces build hassle:
opencv-python-headless

# Model context
fastmcp 
pillow 